using System;
using System.Collections.Generic;
using System.Text;

namespace BProfTemaLab.Dal.Users
{
    class Roles
    {
        public const string Administrators = nameof(Administrators);
        public const string Suppliers = nameof(Suppliers);
        public const string Workers = nameof(Workers);
    }
}
